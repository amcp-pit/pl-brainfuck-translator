p += 3;
*p += 1;
p += 5;
*p += 1;
p += 2;
*p += 1;
p += 2;
*p += 1;
if (*p) do {
  p -= 2;
} while (*p);
*p = getchar();
if (*p) do {
  *p -= 1;
  if (*p) do {
    *p -= 1;
    if (*p) do {
      *p -= 1;
      if (*p) do {
        *p -= 1;
        if (*p) do {
          *p -= 1;
          if (*p) do {
            *p -= 1;
            if (*p) do {
              *p -= 1;
              if (*p) do {
                *p -= 1;
                if (*p) do {
                  p -= 1;
                  *p += 1;
                  p += 1;
                  *p -= 1;
                  if (*p) do {
                    p += 1;
                    *p += 1;
                    p -= 1;
                    *p -= 1;
                    if (*p) do {
                      p += 1;
                      *p -= 1;
                      p -= 1;
                      *p -= 1;
                      if (*p) do {
                        *p -= 1;
                        if (*p) do {
                          *p -= 1;
                          if (*p) do {
                            p -= 1;
                            *p += 2;
                            if (*p) do {
                              p -= 1;
                              *p += 6;
                              p += 1;
                              *p -= 1;
                            } while (*p);
                            p -= 1;
                            if (*p) do {
                              p += 2;
                              if (*p) do {
                                *p -= 1;
                                p -= 1;
                              } while (*p);
                              p -= 1;
                              if (*p) do {
                                p += 1;
                              } while (*p);
                              p -= 1;
                              *p -= 1;
                            } while (*p);
                            p += 2;
                            if (*p) do {
                              p -= 1;
                              *p += 1;
                              p += 1;
                              *p -= 1;
                              if (*p) do {
                                p -= 1;
                                *p -= 1;
                                p += 1;
                                if (*p) do {
                                  *p -= 1;
                                } while (*p);
                              } while (*p);
                            } while (*p);
                          } while (*p);
                        } while (*p);
                      } while (*p);
                    } while (*p);
                  } while (*p);
                } while (*p);
              } while (*p);
            } while (*p);
          } while (*p);
        } while (*p);
      } while (*p);
    } while (*p);
  } while (*p);
  p -= 1;
  if (*p) do {
    *p -= 1;
    p -= 2;
    if (*p) do {
      *p -= 1;
    } while (*p);
    *p += 1;
    p += 1;
  } while (*p);
  p -= 2;
  if (*p) do {
    p += 6;
    *p += 1;
    p -= 6;
    *p -= 1;
  } while (*p);
  p += 1;
  if (*p) do {
    p += 1;
  } while (*p);
  p += 7;
  *p += 1;
  p += 1;
  if (*p) do {
    p -= 1;
    *p += 1;
    if (*p) do {
      p += 1;
      *p += 9;
      p -= 1;
      *p -= 1;
      if (*p) do {
        p += 1;
        *p -= 1;
        p -= 1;
        *p -= 1;
      } while (*p);
      *p += 2;
      p += 1;
      if (*p) do {
        p -= 1;
        *p += 7;
        p += 1;
        *p -= 1;
        if (*p) do {
          p -= 1;
          *p -= 1;
          p += 1;
          *p -= 1;
        } while (*p);
        *p += 1;
        if (*p) do {
          *p += 1;
          p += 6;
        } while (*p);
      } while (*p);
      p -= 1;
      if (*p) do {
        p += 1;
        *p += 1;
        p -= 1;
        *p -= 1;
      } while (*p);
      p += 1;
      if (*p) do {
        p += 5;
        *p += 2;
        p += 1;
        if (*p) do {
          *p -= 1;
        } while (*p);
      } while (*p);
      *p += 1;
      p -= 1;
    } while (*p);
    p += 1;
    if (*p) do {
      *p -= 1;
      p -= 6;
    } while (*p);
    p += 4;
  } while (*p);
  *p = getchar();
} while (*p);
*p += 1;
p -= 1;
*p += 2;
p += 3;
if (*p) do {
  if (*p) do {
    *p += 5;
    p += 6;
  } while (*p);
  p -= 1;
  *p += 1;
  p += 1;
  *p += 1;
  if (*p) do {
    if (*p) do {
      p -= 1;
      *p += 8;
      p += 1;
      *p -= 1;
    } while (*p);
    p -= 1;
    putchar(*p);
    p -= 5;
  } while (*p);
  p += 8;
} while (*p);
